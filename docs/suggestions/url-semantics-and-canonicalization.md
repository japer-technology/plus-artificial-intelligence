# URL Semantics and Canonicalisation

## Suggestion

Use paths for durable resource identity, queries for public presentation and
filters, fragments for bounded local drafts, and linked manifests for structured
declaration meaning.

## Before implementation

Define encoding, size limits, parameter order, defaults, aliases, slugs,
collisions, immutable IDs, redirects, versioning, indexing, and archival policy.
